dependencies {
    implementation files('libs/oscarJDBC8.jar')
}

tasks.named('shadowJar') {
    manifest {
        attributes(
            'Agent-Label': '\u795e\u901a OSCAR',
            'Main-Class': 'org.mutantcat.agent.oscar.OscarAgent'
        )
    }
}
