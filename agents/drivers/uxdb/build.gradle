dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

tasks.named('shadowJar') {
    manifest {
        attributes('Agent-Label': 'UXDB', 'Main-Class': 'org.mutantcat.agent.uxdb.UxdbAgent')
    }
}
