dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'org.firebirdsql.jdbc:jaybird:6.0.5'
}

tasks.named('shadowJar') {
    manifest {
        attributes('Agent-Label': 'Firebird', 'Main-Class': 'org.mutantcat.agent.firebird.FirebirdAgent')
    }
}
